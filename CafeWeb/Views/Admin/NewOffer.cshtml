<!-- Страница для добавления новых акций -->
@model CafeWeb.Models.AdminOfferModel
<form method="post" action="/Admin/NewOffer">
    @if (Model.Foods != null && Model.Foods.Any())
    {
        <select multiple name="FoodIds">
            @foreach (var kvp in Model.Foods)
            {
                <option value="@kvp.Key">@kvp.Value</option>
            }
        </select> <br />
    }
    else
    {
        <p>Не удалось получить блюда</p> <br />
    }
    <label> Название акции: </label> <br />
    <input type="text" name="Name" value="@Model?.Offer?.Name" /> <br />

    <label>Описание акции: </label> <br />
    <textarea name="Description" value="@Model?.Offer?.Description"></textarea> <br />

    <label>Скидка (в процентах): </label> <br />
    <input type="number" min="0" max="100" name="Discount" value="@Model?.Offer?.Discount" /> <br />

    <label>Дата начала акции: </label> <br />
    <input type="date" name="StartsAt" value="@Model?.Offer?.StartsAt" /> <br />

    <label>Дата начала акции: </label> <br />
    <input type="date" name="EndsAt" value="@Model?.Offer?.EndsAt" /> <br />

    <button type="submit">Добавить</button>
</form>